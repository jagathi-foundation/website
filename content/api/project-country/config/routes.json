{
  "routes": [
    {
      "method": "GET",
      "path": "/project-countries",
      "handler": "project-country.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-countries/count",
      "handler": "project-country.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-countries/:id",
      "handler": "project-country.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/project-countries",
      "handler": "project-country.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/project-countries/:id",
      "handler": "project-country.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/project-countries/:id",
      "handler": "project-country.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
